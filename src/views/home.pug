doctype html
html(lang="en")
  head
    title Home
    include head.pug
    link(href="/tabulator_bootstrap5.min.css", rel="stylesheet")
    link(href="https://unpkg.com/tabulator-tables@5.5.0/dist/css/tabulator.min.css", rel="stylesheet")
    script(type="text/javascript", src="https://unpkg.com/tabulator-tables@5.5.0/dist/js/tabulator.min.js")
  body
    include header.pug
        settings

    .center-home
      .alert.alert-warning.small-screen-warning(role="alert") Zoom out if the table is off screen!
      h1 Upcoming birthdays
      #birthdays
      h1 Home - List of files
      #table

script(src="/js/home.js")

script.
    generateTable(!{JSON.stringify(pages)}, !{JSON.stringify(settings)}, "!{settings['url']()}")
    displayBirthdays(!{JSON.stringify(birthdays)})
